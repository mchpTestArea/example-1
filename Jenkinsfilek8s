pipeline {
    agent {
        kubernetes {
            label 'relative'
			defaultContainer 'xc8-mplabx'
            yamlFile 'cloudprovider.yml'
        }
    }
	 stages {
        stage('Checkout') {
            steps {
				checkout scm
            }
        }
		stage('base') {
			steps {
				 build job: "/project/A/${env.BRANCH}", propagate:true, wait: true
			}
		}
	}
}	
def execute(String cmd) {
	if(isUnix()) {
		sh cmd
	} else {
		bat cmd
	}
}